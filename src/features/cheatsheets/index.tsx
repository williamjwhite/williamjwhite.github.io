/**
 * Cheatsheets feature — lazy-loaded entry point.
 * Imported via React.lazy() in App.tsx.
 * Renders as a tab inside the main site — no separate routing needed.
 */
export { CheatsheetApp as default } from './CheatsheetApp'
